version: "3.9"

services:
  frd-agent:
    build: .
    container_name: frd_ai_agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
